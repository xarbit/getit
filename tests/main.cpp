#include <QTest>

#include "domain/requests/bodies/tst_FormdataRequestBody.cpp"

int main(int argc, char** argv)
{
    int status = 0;
//    status |= QTest::qExec(new tst_FormdataRequestBody, argc, argv);
    // status |= QTest::qExec(new TestObject, argc, argv);
    // status |= QTest::qExec(new ..., argc, argv);

    return status;
}
